"""Conflict Detection Service — Phase 4 stub."""
from __future__ import annotations
from app.core.logging import get_logger
logger = get_logger(__name__)

class ConflictService:
    """
    Detects semantic conflicts between clauses in a document.
    Phase 4 implementation pending.
    """
    def detect_conflicts(self, document_id: str) -> list:
        """Detect clause conflicts. Phase 4."""
        raise NotImplementedError("ConflictService.detect_conflicts: Phase 4.")
